/*
 * @format
 */
import { Base, IAttributes } from "src/entities/Base";
import { User } from "src/entities/User";
import { Product } from "src/entities/Product";
import { Order } from "src/entities/Order";

const entities = [User];

export { IAttributes, Base, entities, User, Product, Order };
